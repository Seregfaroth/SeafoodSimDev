class Configuration {
    private m_prices;
    private m_taxingRate;
    private m_map;
    private m_shipOwner;
    private m_shipPrice;
    private m_aiBuyShipBalance;

    private m_schoolsInOnePlace;
    private m_schoolSize;
    private m_schoolMsy;

    private financialMaxScore;
    private environmentalMaxScore;
    private socialMaxScore;

    constructor() {
    }
    toJSON(): Configuration {
        var ret: Configuration;

        return ret;
    }
    fromJSON(p_object) {
    }
}